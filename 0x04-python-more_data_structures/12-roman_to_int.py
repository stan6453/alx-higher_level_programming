#!/usr/bin/python3

numbers = {
        'I':1,
        'V':5,
        'X':10,
        'L':50,
        'C':100,
        'D':500,
        'M':1000,
        }
seq = ['I','V','X','L','C','D','M']
def roman_to_int(roman_string):
    for index, letter in enumerate(string):
        if numbers[letter] in (1, 10, 100, 1000):
            if string[index+1] == seq.index()
